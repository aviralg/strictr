strict `agnes` <1,2,5,6,9>;
strict `as.dendrogram.twins` <1>;
strict `as.membership` <1,2>;
strict `bannerplot` <3,4,5,6,7,8,9,10,11,13,15,17>;
strict `clara` <1,2,3,4,5,6,8,9,10,11,12>;
strict `clusGap` <1,2,3,4,5,6,7>;
strict `clusplot.default` <1,2,5,6,7,8,9,10,11,14,16,17,18,19,20>;
strict `clusplot.partition` <1,2>;
strict `clusplot` <1>;
strict `coef.hclust` <1>;
strict `coef.twins` <1>;
strict `coefHier` <1>;
strict `daisy` <1,4,9>;
strict `diana` <1,2,5,8>;
strict `ellipsoidhull` <1,2,3,4,5,6>;
strict `ellipsoidPoints` <1,2,3,4>;
strict `fanny` <1,2,3,4,7,8,11,12,13>;
strict `lower.to.upper.tri.inds` <1>;
strict `maxSE` <1,2,3,4>;
strict `meanabsdev` <1>;
strict `mkCheckX` <1,2>;
strict `mona` <1>;
strict `pam` <1,2,3,8,9,12,13,14>;
strict `plot.agnes` <1,2,3,4,5>;
strict `plot.clusGap` <1,2,3,4,5,6,7>;
strict `plot.diana` <1,2,3,4,5,6,7>;
strict `plot.mona` <1,2,3,4,5,6,7,8,9>;
strict `plot.partition` <1,2,3,17>;
strict `plot.silhouette` <1,2,4,5,6,7,8,9,11,12>;
strict `pltree.twins` <1,2,4>;
strict `pltree` <1>;
strict `predict.ellipsoid` <1,2>;
strict `print.agnes` <1>;
strict `print.clusGap` <1,2,3>;
strict `print.diana` <1>;
strict `print.dissimilarity` <1>;
strict `print.ellipsoid` <1,2>;
strict `print.mona` <1>;
strict `print.summary.silhouette` <1>;
strict `silhouette.clara` <1,2,3>;
strict `silhouette.default` <1,2,3>;
strict `silhouette.partition` <1>;
strict `silhouette` <1>;
strict `sizeDiss` <1>;
strict `sortSilhouette` <1>;
strict `summary.agnes` <1>;
strict `summary.clara` <1>;
strict `summary.diana` <1>;
strict `summary.dissimilarity` <1,2>;
strict `summary.fanny` <1>;
strict `summary.pam` <1>;
strict `summary.silhouette` <1,2>;
strict `upper.to.lower.tri.inds` <1>;
strict `volume.ellipsoid` <1,2>;
strict `volume` <1>;