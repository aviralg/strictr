strict `.degree` <1>;
strict `.GCD2` <2>;
strict `.is_zero_polynomial` <1>;
strict `.LCM2` <1,2>;
strict `.polylist_from_list` <1>;
strict `[.polylist` <1,2>;
strict `accumulate` <2,3>;
strict `as.character.polynomial` <1,2>;
strict `as.function.polynomial` <1>;
strict `as.polylist` <1>;
strict `as.polynomial` <1>;
strict `c.polylist` <>;
strict `change.origin` <1,2>;
strict `coef.polynomial` <1>;
strict `deriv.polylist` <1>;
strict `deriv.polynomial` <1>;
strict `GCD.polylist` <>;
strict `GCD` <>;
strict `integral.polylist` <1>;
strict `integral.polynomial` <1,2>;
strict `integral` <1>;
strict `is.polylist` <1>;
strict `is.polynomial` <1>;
strict `LCM.polylist` <>;
strict `LCM` <>;
strict `lines.polynomial` <1,2,3,4>;
strict `Math.polynomial` <1>;
strict `Ops.polynomial` <1,2>;
strict `plot.polylist` <1,5,7,8>;
strict `plot.polynomial` <1,4,5,7,8>;
strict `points.polynomial` <1,2>;
strict `poly.calc` <1,2>;
strict `poly.from.roots` <>;
strict `poly.from.values` <1,2>;
strict `poly.orth` <1,2,3>;
strict `polylist` <>;
strict `polynomial` <1>;
strict `predict.polynomial` <1,2>;
strict `print.polynomial` <1,2,3>;
strict `rep.polylist` <1,2>;
strict `solve.polynomial` <1>;
strict `Summary.polylist` <2>;
strict `summary.polynomial` <1>;
strict `unique.polylist` <1>;