strict `Assays` <2>;
strict `.assays_have_identical_dimnames` <1>;
strict `.bind_assays` <1,2>;
strict `.bind_Assays_objects` <1>;
strict `.cbind.DataFrame` <1,2>;
strict `.cbind.SummarizedExperiment` <1>;
strict `.compare` <2>;
strict `.extract_Assays_subset` <1>;
strict `.from_AnnotatedDataFrame_to_DataFrame` <>;
strict `.from_DataFrame_to_AnnotatedDataFrame` <1>;
strict `.from_GRangesList_to_FeatureData` <1>;
strict `.from_GRanges_to_FeatureData` <1>;
strict `.from_rowRanges_to_FeatureData` <>;
strict `geneRangeMapper` <>;
strict `.get_colnames_from_assays` <1>;
strict `.get_rownames_from_assays` <1>;
strict `makeSummarizedExperimentFromDataFrame` <3,4>;
strict `makeSummarizedExperimentFromExpressionSet` <2>;
strict `makeSummarizedExperimentFromLoom` <1,2,3>;
strict `naiveRangeMapper` <1>;
strict `.new_RangedSummarizedExperiment` <1,2,4>;
strict `new_SummarizedExperiment` <1>;
strict `normarg_assays` <2>;
strict `probeRangeMapper` <1>;
strict `.rbind.SummarizedExperiment` <1>;
strict `.replace_Assays_subset` <1,4>;
strict `.ShallowSimpleListAssays0` <>;
strict `SummarizedExperiment` <>;
strict `.SummarizedExperiment.charbound` <1>;
strict `.valid.Assays` <>;
strict `.valid.RangedSummarizedExperiment` <>;
strict `.valid.SummarizedExperiment` <>;
strict `.valid.SummarizedExperiment.assays_dim` <>;
strict `.valid.SummarizedExperiment.assays_ncol` <1>;
strict `.valid.SummarizedExperiment.assays_nrow` <>;