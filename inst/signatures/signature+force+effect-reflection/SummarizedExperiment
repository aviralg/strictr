strict `Assays` <1>;
strict `.assays_have_identical_dimnames` <0>;
strict `.assays_have_identical_dimnames` `normalize_dimnames` <>;
strict `.bind_assays` <0,1>;
strict `.bind_Assays_objects` <0>;
strict `.cbind.DataFrame` <0,1>;
strict `.cbind.SummarizedExperiment` <0>;
strict `.compare` <1>;
strict `.extract_Assays_subset` <0>;
strict `.extract_Assays_subset` `extract_assay_subset` <0>;
strict `.from_AnnotatedDataFrame_to_DataFrame` <>;
strict `.from_DataFrame_to_AnnotatedDataFrame` <0>;
strict `.from_GRangesList_to_FeatureData` <0>;
strict `.from_GRanges_to_FeatureData` <0>;
strict `.from_rowRanges_to_FeatureData` <>;
strict `geneRangeMapper` <>;
strict `.get_colnames_from_assays` <0>;
strict `.get_rownames_from_assays` <0>;
strict `makeSummarizedExperimentFromDataFrame` <2,3>;
strict `makeSummarizedExperimentFromExpressionSet` <1>;
strict `makeSummarizedExperimentFromLoom` <0,1,2>;
strict `naiveRangeMapper` <0>;
strict `.new_RangedSummarizedExperiment` <0,1,3>;
strict `new_SummarizedExperiment` <0>;
strict `normarg_assays` <1>;
strict `probeRangeMapper` <0>;
strict `.rbind.SummarizedExperiment` <0>;
strict `.replace_Assays_subset` <0,3>;
strict `.replace_Assays_subset` `replace_assay_subset` <0,1>;
strict `.ShallowSimpleListAssays0` <>;
strict `SummarizedExperiment` <1>;
strict `.SummarizedExperiment.charbound` <0>;
strict `.valid.Assays` <>;
strict `.valid.RangedSummarizedExperiment` <>;
strict `.valid.SummarizedExperiment` <>;
strict `.valid.SummarizedExperiment.assays_dim` <>;
strict `.valid.SummarizedExperiment.assays_ncol` <0>;
strict `.valid.SummarizedExperiment.assays_nrow` <>;